package com.fabisoft.personal.resource;

import org.springframework.stereotype.Controller;

@Controller
//@RequestMapping("registroaulas")
public class RegistroAulaResource {
	/*
	@Autowired
	RegistroAulaRepository rar;
	// listar todos
	@GetMapping("/listar")
	public List<RegistroAula> listar() {
	List<RegistroAula> ra = rar.listar();	
		return ra;
		
	}
	//salvar
	@PostMapping("/gravar")
	public RegistroAula gravar(@RequestBody RegistroAula r) {
		 r = rar.salvar(r);
		return r;	
		}
	
	// buscar id
	@GetMapping(path = {"/{id}"})
	public RegistroAula buscarId(@PathVariable Long id) {
	RegistroAula a = rar.listarById(id);
	return a;
	}
	// atualizar
	@PutMapping("/{id}")
	public RegistroAula atualizar(@PathVariable("id") long id,@RequestBody RegistroAula r) {
	 r = rar.atualizer(r);
	 return r;
	}
	//deletar
	@DeleteMapping(path = {"/{id}"})
	public String delete(@PathVariable Long id) {
	RegistroAula a = rar.deletebyId(id);
	return "OK";
	
	
		
	}
	*/

}
