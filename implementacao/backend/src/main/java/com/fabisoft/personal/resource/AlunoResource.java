package com.fabisoft.personal.resource;

//@RestController
//@RequestMapping("/alunos")
public class AlunoResource {
	/*
	@Autowired
	private AlunoRepository alunoRepository;
	
	// listar todos
	//@GetMapping()
	public List<Aluno> listar(){
	List<Aluno> alunos = alunoRepository.listar();
		return alunos;
	}
	// listar id
	//@GetMapping("/{id}")
	public Aluno buscarId(@PathVariable Long id) {
	Aluno a = alunoRepository.listarById(id);
	return a;
	}
	// salvar
	@PostMapping
	public Aluno gravar(@RequestBody Aluno aluno) {
		aluno = alunoRepository.gravar(aluno);
		
		return aluno;
	}
	
    @DeleteMapping("/{id}")
	public String deleteById(@PathVariable long id) {
		Aluno a = alunoRepository.deleteById(id);
		return "OK";
	}
	
	*/
	
	
}
